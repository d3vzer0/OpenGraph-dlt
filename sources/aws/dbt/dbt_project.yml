name: 'aws_project'
version: '1.0.0'
config-version: 2

profile: 'default'

model-paths: ["models"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  aws_project:
    staging:
      +materialized: view
      +schema: staging
    # marts:
    #   +materialized: view
    #   +schema: marts